{
    "name": "red-tetris-backend",
    "version": "0.0.1",
    "description": "my first socket.io app",
    "scripts": {
        "dev": "nodemon --legacy-watch src/main.js",
        "test": "jest",
        "testlive": "jest --watchAll",
        "coverage": "jest --coverage",
        "coverage2": "jest --no-cache --coverage --detectOpenHandles",
        "prettier": "prettier --ignore-unknown --write src"
    },
    "dependencies": {
        "express": "4",
        "pg": "^8.12.0",
        "socket.io": "^4.7.5"
    },
    "devDependencies": {
        "jest": "^29.7.0",
        "nodemon": "^3.1.4",
        "prettier": "3.3.3",
        "socket.io-client": "^4.7.5"
    }
}